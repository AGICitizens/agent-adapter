# -----------------------------------------------------------------------------
# Networks
# -----------------------------------------------------------------------------
# 0G Galileo testnet — escrow + x402 settlement live here.
ZG_RPC_URL=https://evmrpc-testnet.0g.ai
ZG_ESCROW_ADDRESS=

# Ethereum Sepolia — ENS identity layer lives here.
SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com

# -----------------------------------------------------------------------------
# Wallet
# -----------------------------------------------------------------------------
PRIVATE_KEY=

# -----------------------------------------------------------------------------
# ENS (Sepolia)
# -----------------------------------------------------------------------------
# Parent name owned by the provider. Subnames register under this on Sepolia.
ENS_PARENT_NAME=

# -----------------------------------------------------------------------------
# KeeperHub
# -----------------------------------------------------------------------------
KEEPERHUB_API_KEY=
KEEPERHUB_BASE_URL=https://app.keeperhub.com/api
# Slug of the workflow you create in the KeeperHub dashboard to publish ENS
# subname text records on Sepolia (e.g. agent-adapter-publish-identity).
KEEPERHUB_IDENTITY_WORKFLOW_SLUG=

# -----------------------------------------------------------------------------
# LLM (OpenRouter)
# -----------------------------------------------------------------------------
AI_PROVIDER=openrouter
OPENROUTER_API_KEY=
LLM_MODEL=google/gemini-2.0-flash-001

# -----------------------------------------------------------------------------
# Upstream provider (the API the adapter wraps)
# -----------------------------------------------------------------------------
UPSTREAM_BASE_URL=
UPSTREAM_OPENAPI_URL=
