/**
 * OWS (Open Wallet Standard) wallet plugin — default for self-hosted mode.
 * Derives addresses for EVM, Solana, and other chains from a single seed.
 * Wraps @open-wallet-standard/core to conform to WalletPlugin interface.
 */

export {};
