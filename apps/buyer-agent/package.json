{
  "name": "@agent-adapter/buyer-agent",
  "version": "0.1.0",
  "private": true,
  "description": "LLM-driven buyer agent — discovers providers via ENS, pays through x402 + escrow, and consumes capabilities autonomously.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "buyer-agent": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsc -p tsconfig.json --watch",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@agent-adapter/contracts": "workspace:*",
    "@agent-adapter/ens-identity": "workspace:*",
    "@agent-adapter/wallet-evm": "workspace:*",
    "openai": "^4.79.0",
    "viem": "^2.21.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
