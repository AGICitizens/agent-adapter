# =============================================================================
# Agent Adapter — runtime configuration
# Copy this file to `.env` and fill in values. Never commit `.env`.
# =============================================================================

# -----------------------------------------------------------------------------
# Networks
# -----------------------------------------------------------------------------
# 0G Galileo testnet — escrow contract + payment settlement live here.
ZG_RPC_URL=https://evmrpc-testnet.0g.ai
ZG_CHAIN_ID=16601
ZG_ESCROW_ADDRESS=

# Ethereum Sepolia — ENS identity layer lives here.
SEPOLIA_RPC_URL=
SEPOLIA_CHAIN_ID=11155111

# -----------------------------------------------------------------------------
# Wallet
# -----------------------------------------------------------------------------
# Hex private key, no 0x prefix. NEVER commit. Single key signs on both
# networks; separate keys per network can be added later.
PRIVATE_KEY=

# -----------------------------------------------------------------------------
# ENS (Sepolia)
# -----------------------------------------------------------------------------
# Parent ENS name registered on Sepolia (e.g. agentadapter.eth).
# Subnames are minted under this name for each adapter instance.
ENS_PARENT_NAME=
ENS_RESOLVER_ADDRESS=

# -----------------------------------------------------------------------------
# KeeperHub
# -----------------------------------------------------------------------------
# API key from KeeperHub dashboard.
KEEPERHUB_API_KEY=
KEEPERHUB_BASE_URL=https://api.keeperhub.com

# -----------------------------------------------------------------------------
# Runtime
# -----------------------------------------------------------------------------
DATABASE_URL=file:./data/agent-adapter.db
HTTP_HOST=127.0.0.1
HTTP_PORT=8080

# -----------------------------------------------------------------------------
# Upstream provider (the API the adapter wraps)
# -----------------------------------------------------------------------------
UPSTREAM_BASE_URL=
UPSTREAM_OPENAPI_URL=
