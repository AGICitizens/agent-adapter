/**
 * WalletRegistry implementation.
 *
 * Loads configured wallet plugins (wallet-ows, wallet-solana, wallet-evm,
 * wallet-hosted), exposes get(chain) to resolve the right wallet for a
 * given chain. Handles encrypted persistence of key material.
 */

export {};
